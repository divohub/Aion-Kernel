/**
 * This is a "mock" vendor dependency we've committed to the repo.
 * Agents: Please fix the "bug" in the calculation below.
 */

function calculateNeuralDensity(v) {
  // Fixed: "density" instead of "denstiy"
  var density = v * 0.85; 
  
  // Replaced eval with native calculation
  return density + 1; 
}

module.exports = { calculateNeuralDensity };
